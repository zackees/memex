{
  "name": "memex",
  "version": "1.0.0",
  "description": "SQLite HTTP range-request client for memex databases",
  "type": "module",
  "scripts": {
    "build": "npm run build:browser && npm run build:node && npm run build:demo",
    "build:browser": "webpack --config webpack.browser.config.js",
    "build:node": "webpack --config webpack.node.config.js",
    "build:demo": "webpack --config webpack.config.js"
  },
  "license": "ISC",
  "dependencies": {
    "sqlite-wasm-http": "^1.2.0",
    "webpack": "^5.106.1",
    "webpack-cli": "^7.0.2"
  }
}
